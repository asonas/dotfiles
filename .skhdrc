#------------------------------------------------------------
# モニター操作
#------------------------------------------------------------
# モニターを選択する
alt - j  : yabai -m display --focus 1
alt - k  : yabai -m display --focus 2
alt - l  : yabai -m display --focus 3

# ウィンドウをモニターに移動する
ctrl + shift - 1  : yabai -m window --display 1; yabai -m display --focus 1
ctrl + shift - 2  : yabai -m window --display 2; yabai -m display --focus 2
ctrl + shift - 3  : yabai -m window --display 3; yabai -m display --focus 3

#------------------------------------------------------------
# ウィンドウの選択・操作
#------------------------------------------------------------
# ウィンドウを選択する
ctrl + shift - j : yabai -m window --focus prev
ctrl + shift - k : yabai -m window --focus next

# ウィンドウを入れ替える
ctrl + alt - b : yabai -m window --swap west
ctrl + alt - n : yabai -m window --swap south
ctrl + alt - p : yabai -m window --swap north
ctrl + alt - f : yabai -m window --swap east

# オフセット有無
alt - a : yabai -m space --toggle padding; yabai -m space --toggle gap

#------------------------------------------------------------
# ウィンドウのサイズ調整
#------------------------------------------------------------
# ウィンドウサイズを全て等しくする
ctrl + alt + shift - 0 : yabai -m space --balance

# 左半分にする
ctrl + alt - left : yabai -m window --grid 1:2:0:0:1:1
ctrl + alt + shift - left : yabai -m window --grid 12:12:0:0:9:12

# 右半分にする
ctrl + alt - right : yabai -m window --grid 1:2:1:0:1:1
ctrl + alt + shift - right : yabai -m window --grid 12:12:3:0:9:12

ctrl + alt - up   : yabai -m window --grid 2:1:0:0:1:1
ctrl + alt - down : yabai -m window --grid 12:12:0:0:12:10


ctrl + alt + shift - right : yabai -m window --space code --grid 1:2:1:0:1:1

# サブディスプレイの上中央に配置（Discordと同じ 1140x720px）
ctrl + shift - 7 : \
  window_id=$(yabai -m query --windows --window | jq '.id'); \
  display_count=$(yabai -m query --displays | jq "length"); \
  if [ $display_count -gt 1 ]; then target_display=2; else target_display=1; fi; \
  display_frame=$(yabai -m query --displays --display $target_display | jq '.frame'); \
  display_x=$(echo "$display_frame" | jq '.x'); \
  display_y=$(echo "$display_frame" | jq '.y'); \
  display_w=$(echo "$display_frame" | jq '.w'); \
  window_w=1140; \
  window_h=720; \
  new_x=$(echo "$display_x + ($display_w - $window_w) / 2" | bc); \
  new_y=$display_y; \
  yabai -m window "$window_id" --toggle float; \
  yabai -m window "$window_id" --display $target_display; \
  yabai -m window "$window_id" --resize abs:"$window_w":"$window_h"; \
  yabai -m window "$window_id" --move abs:"$new_x":"$new_y";

# ウィンドウを移動する
shift + ctrl - a : yabai -m window --move rel:-20:0
shift + ctrl - s : yabai -m window --move rel:0:20
shift + ctrl - w : yabai -m window --move rel:0:-20
shift + ctrl - d : yabai -m window --move rel:20:0

# ウィンドウのサイズを増やす
shift + alt - a : yabai -m window --resize left:-20:0
shift + alt - s : yabai -m window --resize bottom:0:20
shift + alt - w : yabai -m window --resize top:0:-20
shift + alt - d : yabai -m window --resize right:20:0

# set insertion point in focused container
ctrl + alt - h : yabai -m window --insert west
ctrl + alt - j : yabai -m window --insert south
ctrl + alt - k : yabai -m window --insert north
ctrl + alt - l : yabai -m window --insert east

ctrl + shift - 4 : \
  title_bar=28; \
  content_width=1920; \
  content_height=1080; \
  window_height=$((content_height + title_bar)); \
  window_id=$(yabai -m query --windows --window | jq '.id'); \
  display_frame=$(yabai -m query --displays --display 2 | jq '.frame'); \
  display_x=$(echo "$display_frame" | jq '.x'); \
  display_y=$(echo "$display_frame" | jq '.y'); \
  yabai -m window "$window_id" --toggle float; \
  yabai -m window "$window_id" --display 2; \
  yabai -m window "$window_id" --resize abs:"$content_width":"$window_height"; \
  yabai -m window "$window_id" --move abs:"$display_x":"$display_y"; \
  yabai -m display --focus 2;

ctrl + shift - 8 : yabai -m window --display 2 --grid 18:1:0:10:1:9
ctrl + shift - 9 : yabai -m window --move abs:$(yabai -m query --windows --window | jq -r '.frame.x'):0; yabai -m window --resize abs:$(yabai -m query --windows --window | jq -r '.frame.w'):$(yabai -m query --displays --display | jq -r '.frame.h')

# 左右に移動させる
ctrl + shift + alt - right : yabai -m window --move abs:$(($(yabai -m query --displays --display | jq -r '.frame.w') - $(yabai -m query --windows --window | jq -r '.frame.w'))):$(yabai -m query --windows --window | jq -r '.frame.y')
ctrl + shift + alt - left : yabai -m window --move abs:0:$(yabai -m query --windows --window | jq -r '.frame.y')


#------------------------------------------------------------
#   bsp 時のレイアウト操作
#------------------------------------------------------------
# ウインドウの並びを回転する
alt - r : yabai -m space --rotate 90

# Y軸方向で反転する
alt - y : yabai -m space --mirror y-axis

# Y軸方向で反転する
alt - x : yabai -m space --mirror x-axis

# 親レイアウト方向で伸ばす
alt - d : yabai -m window --toggle zoom-parent

# フルスクリーンにする
alt - f : yabai -m window --toggle zoom-fullscreen

# 縦分割にする
alt - e : yabai -m window --toggle split

# 画面中央に表示する
alt - t : yabai -m window --toggle float;\
  yabai -m window --grid 12:12:1:0:10:12

# 画面中央に表示する
alt - h : yabai -m window --toggle float;\
  yabai -m window --resize abs:1620:1280

alt + shift - t : yabai -m window --grid 5:1:2:1:3:3

#------------------------------------------------------------
# Slack操作
#------------------------------------------------------------
# Slackスレッドウィンドウを配置（660x1169px、マルチ:左下、シングル:右下）
ctrl - y : setup_workspace
